[supervisord]
nodaemon=true
logfile=/dev/stdout
logfile_maxbytes=0
loglevel=warn
user=root
logfile=/home/logio/supervisord.log
pidfile=/var/run/supervisord.pid

loglevel=error

[program:logio]
command=log.io-server